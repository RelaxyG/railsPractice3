<div class="d-flex flex-column text-center mt-5">
  <h1><%="✒"+ @task.title%></h1>
  <h4 class="mb-5"><%=@task.details%></h4>
  <p><%=link_to "Home", tasks_path%></p>
  <p><%=link_to "Edit", edit_task_path%></p>
  <p><%= link_to "Delete #{@task.title}",
            task_path(@task),
            method: :delete,
            data: { confirm: "Are you sure?" } %></p>
</div>
